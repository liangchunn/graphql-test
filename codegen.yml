overwrite: true
schema: "./schema.graphql"
documents: null
generates:
  ./src/graphql/graphql-typings.ts:
    plugins:
      - "typescript-common"
      - "typescript-server"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
